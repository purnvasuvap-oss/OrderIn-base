rules_version = '2';

// This app (end customer) only ever reads images uploaded by the
// restaurant back-office app - it never uploads or deletes files.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
