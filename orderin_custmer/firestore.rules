rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read, write, and delete operations for all collections
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}

